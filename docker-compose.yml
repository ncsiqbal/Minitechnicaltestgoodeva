version: '3.9'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

  frontend:
    build: .
    ports:
      - "5173:5173"
    depends_on:
      - backend
